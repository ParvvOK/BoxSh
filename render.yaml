services:
  # Backend WebSocket Service
  - type: web
    name: boxsh-backend
    env: node
    plan: free
    buildCommand: npm install -g pnpm && pnpm install && cd apps/backend && pnpm run build
    startCommand: cd apps/backend && pnpm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
    healthCheckPath: /
    
  # Frontend Static Site
  - type: web
    name: boxsh-frontend
    env: node
    plan: free
    buildCommand: npm install -g pnpm && pnpm install && cd apps/frontend && pnpm run build
    startCommand: cd apps/frontend && pnpm start
    envVars:
      - key: NODE_ENV
        value: production
      - key: NEXT_PUBLIC_WS_URL
        value: wss://boxsh-backend.onrender.com
